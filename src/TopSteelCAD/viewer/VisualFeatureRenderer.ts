/**
 * VisualFeatureRenderer - Version stub minimale pour compilation
 */
import * as THREE from 'three';

export class VisualFeatureRenderer {
  createVisualFeatures(element: any): THREE.Group {
    console.warn('VisualFeatureRenderer: stub implementation');
    return new THREE.Group();
  }
  
  dispose(): void {
    // Stub
  }
}